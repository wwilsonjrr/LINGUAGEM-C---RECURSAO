/*
9. Faça uma função recursiva que receba um número inteiro positivo par N
   e imprima todos os números pares de 0 até N em ordem crescente.
*/

#include <stdio.h>   // Biblioteca padrão de entrada e saída
#include <stdlib.h>


//Protótipo da função recursiva
void imprimePares(int n); // imprimir é diferente de retornar

//Função principal (main)
int main()
{
    int N;  // Variável para armazenar o número digitado

    // Solicita ao usuário um número inteiro positivo e par
    printf("Digite um numero inteiro positivo e par: ");
    scanf("%d", &N);  // Lê o valor digitado e armazena em N

    // Verifica se o número é positivo e par
    if (N < 0 || N % 2 != 0)
    {
        printf("Por favor, digite um numero positivo e par.\n");
        return 0;  // Encerra o programa se o valor for inválido
    }

    // Exibe mensagem antes da impressão
    printf("Numeros pares de 0 ate %d em ordem crescente:\n", N);

    // Chama a função recursiva
    imprimePares(N);

    printf("\n");  // Pula uma linha após a impressão

    system("pause");
    return 0;  // Indica que o programa terminou corretamente
}

// Função recursiva que imprime os números pares de 0 até N em ordem crescente
void imprimePares(int n) // imprimir é diferente de retornar
{
    // Caso base: quando n for 0, imprime 0 e encerra a recursão
    if (n == 0)
    {
        printf("%d ", 0);  // 0 é o primeiro número par
        return;            // Encerra a função
    }

    // Chamada recursiva: imprime os pares até (n - 2)
    imprimePares(n - 2);

    // Após retornar das chamadas anteriores, imprime o valor atual de n
    printf("%d ", n);
}
