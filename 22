/*
22. Uma sequência de Fibonacci generalizada, de f0 a f1, é definida como:

    fibg(f0, f1, 0) = f0
    fibg(f0, f1, 1) = f1
    fibg(f0, f1, n) = fibg(f0, f1, n - 1) + fibg(f0, f1, n - 2), se n > 1.

    Assim, fibg(f0, f1, 0), fibg(f0, f1, 1), fibg(f0, f1, 2), ...
    formam uma sequência de Fibonacci a partir de dois valores iniciais fornecidos.
*/

#include <stdio.h>   // Biblioteca padrão de entrada e saída
#include <stdlib.h>

// Protótipo da função recursiva
int fibg(int F0, int F1, int N);

// Função principal (main)
int main()
{
    int f0, f1, n;  // Declara as variáveis para armazenar os valores iniciais e o termo desejado

    // Solicita que o usuário digite os dois primeiros termos da sequência
    printf("Digite o primeiro termo (f0): ");
    scanf("%d", &f0);  // Lê e armazena o valor de f0

    printf("Digite o segundo termo (f1): ");
    scanf("%d", &f1);  // Lê e armazena o valor de f1

    // Solicita que o usuário digite o índice do termo desejado
    printf("Digite o valor de n (indice do termo desejado): ");
    scanf("%d", &n);  // Lê e armazena o valor de n

    // Verifica se n é válido (não negativo)
    if (n < 0)
    {
        printf("O valor de n deve ser maior ou igual a zero.\n");  // Mensagem de erro
        return 0;  // Encerra o programa se o valor for inválido
    }

    // Chama a função recursiva e armazena o resultado
    int resultado = fibg(f0, f1, n);

    // Exibe o resultado final
    printf("\nO termo fibg(%d, %d, %d) eh: %d\n", f0, f1, n, resultado);

    system("pause");
    return 0;  // Indica que o programa terminou corretamente
}

// Função recursiva que calcula o n-ésimo termo da sequência de Fibonacci generalizada
int fibg(int F0, int F1, int N)
{
    // Caso base 1: se n for 0, retorna o primeiro termo f0
    if (N == 0)
        return F0;  // Retorna o valor inicial f0

    // Caso base 2: se n for 1, retorna o segundo termo f1
    if (N == 1)
        return F1;  // Retorna o valor inicial f1

    // Caso recursivo: soma os dois termos anteriores da sequência
    return fibg(F0, F1, N - 1) + fibg(F0, F1, N - 2);
}
