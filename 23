/*
23. Faça uma função recursiva que permita somar os elementos de um vetor de inteiros.


    Se o vetor for:

    [2, 4, 6, 8, 10]


    A função faz:

    somaVetor(v, 5)
    = 10 + somaVetor(v, 4)
    = 10 + 8 + somaVetor(v, 3)
    = 10 + 8 + 6 + somaVetor(v, 2)
    = 10 + 8 + 6 + 4 + somaVetor(v, 1)
    = 10 + 8 + 6 + 4 + 2

*/

#include <stdio.h>   // Biblioteca padrão de entrada e saída
#include <stdlib.h>

// -----------------------------------------------------------------------------
// Função recursiva que soma os elementos de um vetor (VERSÃO DIDÁTICA)
// Caso base: vetor com apenas um elemento
// -----------------------------------------------------------------------------
int somaVetor(int vetor[], int tamanho)
{
    // Caso base: se o vetor tiver apenas um elemento,
    // esse elemento é a própria soma
    if (tamanho == 1)
        return vetor[0];

    // Caso recursivo:
    // soma o último elemento com a soma dos anteriores
    return vetor[tamanho - 1] + somaVetor(vetor, tamanho - 1);
}

// -----------------------------------------------------------------------------
// Função principal
// -----------------------------------------------------------------------------
int main()
{
    int vetor[5];  // Declara um vetor com 5 posições
    int i;         // Variável auxiliar para leitura dos valores

    // Solicita que o usuário digite os 5 elementos do vetor
    printf("Digite 5 numeros inteiros:\n");
    for (i = 0; i < 5; i++)
    {
        printf("Elemento %d: ", i + 1);
        scanf("%d", &vetor[i]);
    }

    // Chama a função recursiva para somar todos os elementos
    int soma = somaVetor(vetor, 5);

    // Exibe o resultado final
    printf("\nA soma dos elementos do vetor eh: %d\n", soma);

    system("pause");
    return 0;
}
