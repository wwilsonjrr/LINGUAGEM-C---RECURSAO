/*
21. Escreva uma função recursiva que calcule a sequência dada por:

    F(1) = 1
    F(2) = 2
    F(n) = 2 * F(n - 1) + 3 * F(n - 2)



    F(3) = 2 * F(2) + 3 * F(1)
    F(3) = 2 * 2 + 3 * 1 = 7

    F(4) = 2 * F(3) + 3 * F(2)
    F(4) = 2 * 7 + 3 * 2 = 20

                .
                .
                .
*/

#include <stdio.h>   // Biblioteca padrão de entrada e saída
#include <stdlib.h>

// Protótipo da função recursiva
int F(int N);

// Função principal (main)
int main()
{
    int n;  // Declara a variável para armazenar o valor digitado pelo usuário

    // Solicita que o usuário digite o termo desejado da sequência
    printf("Digite o valor de n (n >= 1): ");
    scanf("%d", &n);  // Lê o valor digitado e armazena em n

    // Verifica se o valor de n é válido (n deve ser positivo)
    if (n <= 0)
    {
        printf("O valor de n deve ser maior ou igual a 1.\n");  // Mensagem de erro
        return 0;  // Encerra o programa se o valor for inválido
    }

    // Chama a função recursiva F e armazena o resultado em resultado
    int resultado = F(n);

    // Exibe o resultado final da sequência
    printf("\nO valor de F(%d) eh: %d\n", n, resultado);

    system("pause");
    return 0;  // Indica que o programa terminou corretamente
}

// Função recursiva que calcula o termo F(n) da sequência
int F(int N)
{
    // Caso base 1: quando n for igual a 1, retorna 1
    if (N == 1)
        return 1;  // F(1) = 1

    // Caso base 2: quando n for igual a 2, retorna 2
    if (N == 2)
        return 2;  // F(2) = 2

    // Caso recursivo: aplica a fórmula F(n) = 2 * F(n - 1) + 3 * F(n - 2)
    return 2 * F(N - 1) + 3 * F(N - 2);
}
